# Cards

C project for myself, for learning non-integer array manipulation.

Contains both `deck.c` and `deck.lua`.

`deck.lua` replicates the functionality I want to implement in `deck.c`.

Currently unfinished.
